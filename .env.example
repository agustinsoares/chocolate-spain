# Cliente (Vite)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# API (Vercel / server)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SITE_URL=http://localhost:8080

# Resend — para recuperación de contraseña y mails de pedido
RESEND_API_KEY=
# Con dominio verificado: Chocolate <hola@tudominio.com>
# Sin dominio (solo pruebas): Chocolate Spain <onboarding@resend.dev>
RESEND_FROM=Chocolate Spain <onboarding@resend.dev>

# Stripe (opcional / deshabilitado en checkout por ahora)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Supabase Dashboard → Authentication → URL Configuration → Redirect URLs:
#   http://localhost:8080/nueva-contrasena
#   https://tu-dominio.com/nueva-contrasena
